# Thisari Siriwardana Portfolio

## Development
1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## Build
- Create a production bundle:
  `npm run build`
- Preview the build locally:
  `npm run preview`
